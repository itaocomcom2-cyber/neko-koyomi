# 猫の暦 — 誤りを検査して直し、全153日の内容を書き直した回の記録

2026-08-30 の作業記録。前回は 2026-08-12（[9〜12月の追加](./セッション記録_2026-08-12.md) と [文字サイズ](./セッション記録_2026-08-12_文字サイズ.md)）。
暦の計算ルールと恒久的な判断は [CLAUDE.md](./CLAUDE.md) にある。このファイルは「どう調べて、何が出て、どう直したか」の記録。

---

## 依頼

> 作った猫の暦のアプリのエラーはありますか？

（検査結果を報告したあと）

> 修正してください、吉方位、今日のテーマ、今日の言葉が２回出てきます。今日のテーマ、最適な行動をもっと具体的にしてください

> 押してください

つまり3段階：**①誤りの検査 → ②修正（誤り＋重複表示＋内容の具体化）→ ③配信**。

---

## 本体を見つけるまで

今回は迷わなかった。作業ディレクトリは `C:\Users\User\OneDrive\デスクトップ\AI\AIの作業場` で、ここには家計簿・タロット・食事記録しかない。しかし **[AI\00_ここに何があるか.md](../OneDrive/デスクトップ/AI/00_ここに何があるか.md) の「公開しているアプリ（全4本）」の表に `C:\Users\User\neko-koyomi`（**この AI フォルダの外**）と書いてあった**ため、1手で到達できた。

前回2回とも `find` で総当たりしていたので、**あの表を書いておいたことが効いた**。同じ形の索引は今後も維持する価値がある。

---

## どう検査したか

「エラーはありますか」に対して、**思い込みで読むのではなく、機械的に照合できるものは全部照合する**方針をとった。作業用スクリプトは一時フォルダに置き、リポジトリには入れていない（→「次にやるとき」参照）。

### 1. 静的な検査

| 何を | どうやって |
| --- | --- |
| JSの構文 | `<script>` 〜 `</script>` を抜き出して `node --check` |
| JSが参照するID | `getElementById("…")` を全部拾い、HTML側の `id=` と `comm` で突き合わせ |
| ID重複 | HTMLの `id=` を全部拾い、重複しているものを数える |
| 参照先の実体 | `icons/` の4枚、`manifest.webmanifest`、`sw.js` の `ASSETS` |

### 2. 実行時の検査（簡易DOMを自作）

`jsdom` が入っていないので、**必要な分だけのDOMシムを80行ほど書いて Node で本物のスクリプトを走らせた**。

- `document.getElementById` / `createElement` / `querySelectorAll`
- `classList` / `dataset` / `innerHTML` / `appendChild` / `addEventListener`
- `canvas.getContext` はダミー、`toDataURL` は空文字を返す

そのうえで **月タブ5つを順に押し、各月の全日セルをクリックし、QRダイアログ・保存・「今日」ボタン・`load` イベントまで発火**させた。153日ぶんの `renderFeature()` と `select()` を全部通したことになる。

```
月タブ数: 5 → 八月 九月 十月 十一月 十二月
  八月: 日セル 31 / 空白 6 / title「猫の暦 — 2026年8月」
  …
■ 実行時エラーなし
```

### 3. 暦データの照合（153日ぶん）

CLAUDE.md の計算ルールを**別実装して**、データと突き合わせた。データを信じずに再計算するのが要点。

| 項目 | 照合の根拠 |
| --- | --- |
| 日数・1日の曜日 | `new Date(2026, m, 0)` と `getDay()` |
| 干支 | 2026-08-01 = 丁未（番号43）から `(n+i) mod 60` |
| Kin・Kin名 | 8/1 = Kin69 から加算、紋章 `((k-1)%20)+1` / 音 `((k-1)%13)+1` から名前を組み立てて比較 |
| 六曜 | `(旧暦月 + 旧暦日) mod 6` |
| 九星 | 6/19 甲子＝九紫で陰遁開始 → 12/15、12/16 甲子＝一白で陽遁 |
| 十二直 | 前日から1つ進む。節入り日（8/7・9/7・10/8・11/7・12/7）だけ前日と同じ |
| 五行 | 日の十干 |
| 吉方位 | 日の十二支（データから最頻値を取り、外れ値を探す方式） |
| 旧暦の連続性 | 前日+1、月替わりは29日か30日の翌日が1日 |
| kyuKanji | kyu から漢数字を組み立てて比較 |
| 七十二候 | 候の切り替わりが5日前後か、同じ候が離れて再登場しないか |
| 選日7種 | 一粒万倍日・天赦日・不成就日・天恩日・受死日・三隣亡・八専（間日込み）を節月と干支から再計算 |

### 4. 朔弦望の独立計算

月相は暦要項の転記なので、**Meeus『Astronomical Algorithms』第49章を実装して21件すべてを再計算**した。TT→UTC を約70秒、JSTを+9時間として比較。

```
2026-08-13 02:36  朔（新月）      2026-09-27 01:48  望（満月）
2026-09-11 12:27  朔（新月）      2026-10-26 13:11  望（満月）
…
```

**21件すべてデータと一致**。この計算が、あとで中秋の名月の誤りを裏づける根拠になった。

---

## 見つかったもの

### プログラムの誤り：ゼロ

構文・実行時・ID参照・アイコン・配信物、すべて問題なし。**「エラー」はコードにはなかった。**

### 暦データの誤り：2件（どちらも自分自身の旧暦データと矛盾していた）

| | 誤り | 正しい | 根拠 |
| --- | --- | --- | --- |
| 中秋の名月 | 9月27日 | **9月25日** | 中秋の名月＝旧暦8月15日。データ上 旧8/1＝9/11 なので旧8/15＝9/25 |
| 十三夜 | 10月31日 | **10月23日** | 十三夜＝旧暦9月13日。データ上 旧9/1＝10/11 なので旧9/13＝10/23。10/31 は旧9/21 |

**原因は「名月＝満月」と思い込んだこと。** 2026年は 名月9/25 に対して 望9/27 で**二日ずれる**年だった。9/27 が望であること自体はMeeusの計算とも一致しており正しい。誤っていたのは「そこに名月を置いた」判断のほう。

同じ誤りは CLAUDE.md の月相の表（`| 9月 | … | 9/27 中秋の名月 | …`）にも書かれていた。**引き継ぎ文書のほうが誤りの供給源になっていた**ので、そちらも直した。

### 記述の矛盾：4件

1. **8/2 のタグ本文が事実と逆**。8/2 は戊申＝**申の日**で、夏土用の間日（卯・辰・申）に該当するのに「間日にはあたらない日」と書いてあった。同じ日の「注意する行動」にも「土いじり（土用の最中）」が残っていた。
2. **夏土用のタグが 8/1・8/2 の2日だけ**。期間は8/1〜8/6。秋土用は18日全部に付いており不揃い。
3. **秋土用の間日（10/24・10/26・10/28・11/5＝未酉亥の日）が示されていない**。八専は間日を分けているのに、土用だけ一律「土を動かす作業は控える」だった。
4. **manifest の `name` が「猫の暦 2026年8月」のまま**。9〜12月を足したときの更新漏れ。

### 表示の重複（ユーザー指摘）

上段（`feature`）と詳細欄（`detail`）に**同じものが二度出ていた**。指摘は吉方位・テーマ・言霊の3つだったが、調べると6項目あった。

```
テーマ / 九星 / 開運フード / 吉方位 / 七十二候 / 今日の言霊
```

六曜・十二直・マヤ暦も両方に出るが、詳細欄は「意味」や「城」を足しているので重複ではないと判断し、残した。

---

## 直したこと

### 1. 暦データ

- 9/25 に `evt: "中秋の名月"` を追加し、9/27 は `evt: "望（満月）"` に。9月の季節帯へ「月見｜中秋の名月｜9月25日（旧八月十五日）」を追加、望の副題は「牡羊座」に（他月と同じく満月の星座を入れる形式）
- 10/23 を `evt: "霜降・十三夜"` に、10/31 の誤った `evt: "十三夜"` を削除。10月の季節帯へ「月見｜十三夜｜10月23日（旧九月十三日）」を追加
- 8/2 を「夏土用の間日」タグに変更（`k: "good"`）。矛盾していた donts も差し替え
- 8/3〜8/6 に「夏土用」タグを追加（8/6 は最終日である旨を本文に）
- 10/24・10/26・10/28・11/5 を「秋土用の間日」タグに変更し、donts から土用の禁を外した
- 11/6 は土用の最終日なので「禁が解けるのは翌7日の立冬から」と書き分け
- 十三夜を移したことで浮いた言霊を入れ替え（10/31「月に願います」→「静かにおさめます」、10/23 に「月に願います」、9/25 は「月をながめます」）

**タグの命名は八専に合わせた。** 八専が「八専入り／八専／八専間日／八専明け」と分けているので、土用も「夏土用／夏土用の間日」「秋土用／秋土用の間日」に揃えた。

### 2. 表示の分担を決めた

重複を消すにあたって、**どちらが何を持つかを先に決めた**。

| | 役割 | 中身 |
| --- | --- | --- |
| 上段 `feature` | その日のダイジェスト | 六曜・十二直・干支・九星・Kin・候／テーマ／すると良いこと・控えたいこと／開運フード・吉方位・開運色・言霊 |
| 詳細欄 `detail` | 暦の読み解き | 旧暦と干支のよみ／六曜と十二直の**意味**／マヤ暦の**城**／暦注一つずつの説明／五行／縁日 |

詳細欄から6項目を削除し、代わりに**縁日を独立した行に格上げ**した（従来は `d-grid` の中の1マス）。五行には「木＝青菜、火＝赤い食材…」という開運フードの根拠を添えた。

使わなくなった `.d-theme` と `.d-grid` のCSSは削除し、`.d-row.d-sep`（点線の区切り）と `.d-row .d-val.engi`（縁日を朱色に）に置き換えた。

### 3. 見出しを選んだ日にあわせた

今日以外の日を選んでも「**今日の**テーマ」と出ていたのを、`renderFeature()` の `isToday` で切り替えるようにした。

| 元 | 後 |
| --- | --- |
| 今日のテーマ | 今日／**この日**のテーマ |
| 今日の言霊 | 今日／**この日**の言霊 |
| 最適な行動 | 今日／この日に**すると良いこと** |
| 注意する行動 | 今日／この日は**控えたいこと** |

後半2つは重複とは無関係だが、「もっと具体的に」という依頼に沿って、見出し自体を行動の言葉にした。

### 4. 全153日のテーマと行動を書き直した

**元の文面の問題は、暦注が変わっても文言が変わらないこと**だった。「時間を選ばず動ける」「午前のうちに動く」「静かに過ごす」といった汎用句が何十日も使い回されていて、その日を読む意味が薄かった。

方針を3つ決めた。

1. **テーマは「暦注の名前＋なぜそうなるか＋今日の使い方」を一文か二文で。** 抽象的な比喩をやめる
2. **行動は「いつ・何を・どれだけ」を入れる。** 数量（3つ／10着／30分／一か所）と時間帯を必ず添える
3. **六曜は時間帯に、十二直と暦注は行動の中身に対応させる**

六曜と時間帯の対応は全日で統一した。

| 六曜 | 行動に入れる時間帯 |
| --- | --- |
| 先勝 | 午前中に |
| 先負 | 午後1時以降に |
| 赤口 | 11〜13時に |
| 友引 | 朝か夕方に（正午を外す） |
| 大安・仏滅 | 時間の指定なし（大安は「この日のうちに」） |

書き換えの例：

```
8/2  旧: 午前のうちに手放し、身近な人に感謝を返す日。
        ・午前中に用事を片づける
        ・不要品を処分して風通しをよくする

     新: 先勝と十二直「除」。午前のうちに要らないものを取り除く日。
         夏土用の間日なので、土いじりをしても差し支えない。
        ・午前中に、玄関か冷蔵庫のどちらか一か所を空にして拭く
        ・着ていない服を10着選び、処分か寄付にまわす
```

書き換えは**JSONに新旧を持たせて一括適用**した。日ブロックを `      d: N, roku:` で切り出し、その中の `theme:` 行と `dos:` 行だけを差し替える方式（153日を1回で処理、行数は変わらない）。適用時に**引用符とバックスラッシュの混入、テーマ78字・行動36字の超過を検査**している。

書き換え後、**「すると良いこと」に否定形が混じっていた17件**を洗い出して、実際の行動に置き換えた（例：「借入れと分割払いだけは、この日に決めない」→「解約や退会の手続きを、大安のうちに済ませる」）。避けることは donts の役目なので、dos に置かない。

### 5. そのほか

- `manifest.webmanifest` の `name` を「猫の暦 — 開運カレンダー」に（月を書かない形にして、月を足すたびの更新漏れをなくす）
- `sw.js` の `CACHE` を **v3 → v4**
- `CLAUDE.md` に、名月の出し方（旧暦から出す、望とは別物）・土用の間日のルール・表示の分担・書き方の方針を追記

---

## 検証の結果

修正後、検査を最初から全部やり直した。

```
■ 構文OK
■ 実行時エラーなし（5か月・153日の描画、QR、今日ボタン、SW登録まで）
■ 干支・六曜・十二直・九星・Kin・五行・方位・旧暦・七十二候：全153日一致
■ 選日：一粒万倍日26 / 天赦日2 / 不成就日21 / 天恩日41 / 受死日12 / 三隣亡13
        八専22＋間日12：すべて計算と一致
■ 夏土用 5日＋間日1日＝6日、秋土用 14日＋間日4日＝18日
■ 全153日：テーマ・行動3つ すべて揃っている
■ 重複しているテーマ：なし
■ 改行コードの混在なし（全編LF）、文字化けなし
```

---

## コミットと配信

```
d812d18  main → origin/main
```

コミットしたのは **`index.html` / `manifest.webmanifest` / `sw.js` / `CLAUDE.md` の4ファイルだけ**。未追跡の `暦の要素メモ.md` は、**リポジトリがPublicなので意図せず公開しないよう手元に残した**。

配信の確認は、GitHub Pages から取り直して手元と比較した。

- `sw.js` が `neko-koyomi-v4` に切り替わったのを確認（プッシュから約30秒後は v3、その次で v4）
- 配信されている `index.html` が手元と**バイト単位で一致**（212,976バイト、diff差分ゼロ）
- 配信物の中に `evt: "中秋の名月"`（9/25）と `evt: "霜降・十三夜"`（10/23）があることを確認

**ホーム画面に追加済みの端末は、アプリを2回開いたときに反映される**（1回目に裏でダウンロード）。

---

## 次にやるとき

### 検査スクリプトをリポジトリに入れるべき

家計簿（149件）・タロット・食事記録には `〜テスト.js` があるが、**猫の暦にはない**。今回の検査は一時フォルダで組み立てて捨ててしまった。同じものをまた書くのは無駄なので、次に触るときに `暦_テスト.js` として置くとよい。中身は本文の「どう検査したか」の3と4がそのまま使える。

- 干支・Kin・六曜・九星・十二直・五行・方位・旧暦・七十二候の再計算
- 選日7種の再計算
- Meeus による朔弦望の再計算
- 簡易DOMシムでの全日描画

`node 暦_テスト.js index.html` の形にすれば、他の3アプリと手順が揃う。

### 2027年以降を足すときの注意（今回の教訓）

- **名月は旧暦から出す。** 望の日付を見て決めない。中秋の名月＝旧8/15、十三夜＝旧9/13
- **土用は間日を分ける。** 一律に「土いじりは凶」と書かない
- **CLAUDE.md の表を先に疑う。** 今回の誤りは引き継ぎ文書に書かれていた
- テーマと行動を足すときは、**六曜↔時間帯の対応表**（本文「4」）に従う

### 残っている小さな不揃い

- **寅の日・巳の日のタグが8月だけ**（8/8・8/20・8/11）。9〜12月にはない。ただし詳細欄の「縁日」には `engiOf()` が全月自動で出すので、実害は小さい
- 「暦のことば」の縁日の説明に、弘法大師（21日）・地蔵菩薩（24日）・水天宮（5日）が載っていない（`engiOf()` は出している）
