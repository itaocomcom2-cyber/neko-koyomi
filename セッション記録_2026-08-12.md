# 猫の暦 — 2026年9〜12月を追加した回の記録

2026-08-12 の作業記録。何をどう決めて、どこを直したかを残す。
暦の計算ルールそのものは [CLAUDE.md](./CLAUDE.md) にまとめてあるので、次に月を足すときはそちらを見る。このファイルは「なぜそうしたか」と「何を疑って何を確かめたか」の記録。

## 依頼と、返した設計

> 9月から12月分まで作って、その月の頭にアップできるようにしてください

「その月の頭にアップ」ではなく、**5か月分を先に全部入れて、開いた日の月が自動で出る**形にした。理由は、毎月の手作業がなくなり、先の月も前もって見られるため。月替わりに何もしなくてよい。

- 月タブ（八月〜十二月）で手動切り替えもできる
- 実際の日付が2026年8〜12月なら、その月のその日を開く
- 範囲外のときは、2026年12月より後なら十二月、それ以前なら八月の1日

## 作業の順序

1. **公開物の所在を特定** — 手元の `codex` フォルダにはQRとアイコンしかなく、本体は `C:\Users\User\neko-koyomi`（GitHub Pages）だった。9月版はローカルにもArtifactにも存在せず、「作った」ものは残っていなかった。
2. **外部データの取得** — 節気・朔弦望・祝日を国立天文台から取る（後述）。
3. **計算エンジンを実装** — 干支・六曜・十二直・九星・Kin・選日を計算。
4. **既存8月版で検証** — 31日すべてが一致するまで直す。ここで既存データ側の抜けが見つかった。
5. **9〜12月を生成** — 暦は計算、解釈文は十二直×六曜×暦注×季節から組む。
6. **HTMLを月切り替え対応に** — `DAYS` 単体 → `M8`〜`M12` ＋ `MONTHS`。
7. **jsdom で実物を動かして確認** — 曜日・祝日・本日判定を全月分。
8. **公開** — `sw.js` を v2 に上げて push。

## 外部データの出典と、つまずいた点

| 何 | どこ |
| --- | --- |
| 二十四節気・雑節 | https://eco.mtk.nao.ac.jp/koyomi/yoko/2026/rekiyou262.html |
| 朔弦望 | https://eco.mtk.nao.ac.jp/koyomi/yoko/2026/rekiyou263.html |
| 国民の祝日 | https://eco.mtk.nao.ac.jp/koyomi/yoko/2026/rekiyou261.html |

**このページは EUC-JP。** WebFetch にかけると記号が化けて、要約する側が**上弦と下弦を取り違える**。実際、同じページを2回読んで逆の答えが返った。

対処：生HTMLを取って自分でデコードする。

```bash
curl -s "https://eco.mtk.nao.ac.jp/koyomi/yoko/2026/rekiyou263.html" -o sakugenbo.html
node -e "const b=require('fs').readFileSync('sakugenbo.html');
console.log(new TextDecoder('euc-jp').decode(b).replace(/<\/tr>/g,'\n').replace(/<[^>]+>/g,'\t'))"
```

記号が化けても、**並びが「下弦 → 朔 → 上弦 → 望」の繰り返し**なので位置で判別できる。既知の8月（朔8/13・望8/28）を当てて確かめた。

## 計算エンジンの要点

実装は使い捨てのスクリプト（一時フォルダ）で、リポジトリには入れていない。ルールは CLAUDE.md にあるので再実装できる。特に間違えやすい3点だけ再掲する。

**九星の陰遁・陽遁**（ここが最大の落とし穴）

CLAUDE.md には当初「冬至**後**の甲子から陽遁」と書いてあったが、これだと2026年の陽遁始めが翌2027年2月14日になってしまう。正しくは**冬至・夏至に「もっとも近い」甲子**。

```js
const INTON_J  = jdn(2026, 6, 19);   // 陰遁始め：九紫から逆行
const YOUTON_J = jdn(2026, 12, 16);  // 陽遁始め：一白から順行（冬至12/22より6日前）
function kyuseiOf(m, d) {
  const j = jdn(2026, m, d);
  if (j >= YOUTON_J) return KYUSEI[(j - YOUTON_J) % 9];        // 順行
  return KYUSEI[((8 - (j - INTON_J)) % 9 + 9000) % 9];         // 逆行
}
```

検算：6/19（甲子・九紫）から43日逆行すると 8/1 が二黒土星になり、既存8月版と合う。外部でも「2026年12月16日が陽遁始めで一白水星」と確認した。

**八専の間日**

「丑・辰・午・戌の日」と覚えると八専期間外まで拾ってしまう。実体は**干と支の五行が一致しない4日＝癸丑・丙辰・戊午・壬戌**で確定。

**節入り日の扱い**

節入り日はその日から新しい節月に入る。**例外は十二直だけ**で、節入り日は前日と同じ十二直が1日重複する。受死日・三隣亡・一粒万倍日は節入り日から新節月の十二支で判定する（8/7の受死日はこれで決まる）。

## 検証の結果

**既存8月版との突き合わせ（31日）** — 六曜・十二直・干支・読み・旧暦・九星・Kin・七十二候・五行・方位がすべて一致。

**外部の暦との照合**

| 項目 | 計算 | 外部 |
| --- | --- | --- |
| 一粒万倍日 10月 | 1・11・14・23・26 | 一致 |
| 一粒万倍日 11月 | 4・7・8・19・20 | 一致 |
| 一粒万倍日 12月 | 1・2・15・16・27・28 | 一致 |
| 天赦日 | 10/1・12/16 | 一致 |
| 三隣亡 9〜12月 | 13/25、7/11/23、4/9/21、3/18/30 | 一致 |
| 受死日 8月 | 7・19・31 | 一致 |
| 不成就日 8月 | 4・12・15・23・31 | 一致 |

**最終照合** — HTML内の全153日（8〜12月）を計算エンジンと再突合し、全項目一致を確認した。

**実物の動作確認** — jsdom で日付を差し替えて起動し、8/12・9/15・12/16・2027/3の4パターンで、表示月・本日セル・選択日・タイトル・季節帯を確認。各月の1日の曜日と日数は `new Date()` で検算した（8月土曜始まり31日／9月火曜30日／10月木曜31日／11月日曜30日／12月火曜31日）。

## 既存8月版で見つかった抜けと、その根拠

いずれも外部の暦で裏を取ってから直した。

| 日 | 直した内容 | 根拠 |
| --- | --- | --- |
| 8/7 | 受死日を追加 | 立秋当日から申月に入り、丑の日（癸丑）が受死日。外部一覧も「8月の受死日は7・19・31日」 |
| 8/12 | 不成就日を追加 | 旧6/30。旧6月が大の月（7/14〜8/12）なので30日が存在する |
| 8/16 | 八専 → 八専間日 | 壬戌は干支の五行が一致しない4日の一つ |
| 8/17 | 三隣亡を追加 | 申月＝節月7月は亥の日が三隣亡。癸亥が該当。外部一覧も「8月は17日・29日」 |

CLAUDE.md の該当節（九星・八専・受死日・三隣亡・不成就日）も同時に訂正した。

## HTMLの構造変更

```
変更前                          変更後
const DAYS = [31日]        →   const M8 〜 M12 = [各月]
                               const MONTHS = { 8:{days,kanji,wafu,sub,en,firstDow,season}, … }
                               const MLIST = [8,9,10,11,12]
FIRST_DOW（定数）          →   MONTHS[m].firstDow
inAugust / TODAY           →   REAL_M / REAL_D（本当の今日）+ curM（表示中の月）
描画は起動時に1回           →   showMonth(m, d) で見出し・季節帯・カレンダー・詳細を描き直す
「白い北の城」を直書き      →   castleOf(kin) でKinから求める
```

- 月タブは `MLIST` から自動生成。CSSは `.month-nav` / `.mn-btn` / `.mn-btn.on` を追加（選択中は `--ai` 背景＋`--panel` 文字で、ライト／ダーク両方で読める）。
- `renderHead()` が `<title>` とアプリバー副題（`#app-sub`）も書き換える。
- 追加したHTML要素のid：`mh-month` / `mh-wafu` / `app-sub` / `season-bar` / `month-nav`。

## 解釈文の作り方（テーマ・行動・言霊）

暦は計算で出るが、テーマ・最適な行動・注意する行動・言霊・開運フードは解釈。次のように組んだ。

- **土台**は十二直12種 × 各3通りのテーマ、各2通りの行動
- **時間帯**は六曜から1項目（「午前のうちに動く」など）
- **暦注**があれば行動を差し替え（一粒万倍日→積立、天恩日→お礼参り、など）
- **節気・行事・月相の日**（節気、祝日、彼岸、七五三、冬至、大晦日など約35日）は専用の文を用意して上書き
- **開運フード**は日の十干の五行 × その月の旬
- **言霊**は月ごとに30個前後のリストから

破・危・受死日・不成就日といった凶が勝つ日には、吉日由来の前向きな行動を入れない。受死日は十二直より「慎む」を優先する。

品質検査を書いて、**全122日でテーマと行動の重複ゼロ**、他言語文字の混入なし、dos/dontsの矛盾なしを確認した。十二直は12日周期で戻るため、素朴に作ると月内で同じ文が並ぶ。使用済みの文を記録して次の候補にずらす方式で解消した。

## 次にやるとき（2027年以降）

1. 暦要項でその年の節気・朔弦望・祝日を取る（EUC-JPに注意）
2. 朔日から旧暦を組み、**中気を含まない月がないか**＝閏月の有無を確認（2026年後半は閏月なし）
3. CLAUDE.md の各ルールで計算
4. `MONTHS` に月を足して `firstDow` を入れる（タブは自動生成）
5. **`sw.js` の `CACHE` の版数を上げる** ← 忘れるとスマホに古い画面が残る
6. `git add -A && git commit && git push`、反映まで1〜2分

## この回のコミット

```
53261a3 9月〜12月を追加し、月を切り替えて見られるようにした
```

index.html は 77KB → 186KB（153日分）。sw.js は v1 → v2。
