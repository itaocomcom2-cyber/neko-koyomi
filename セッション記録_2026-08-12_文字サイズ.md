# 猫の暦 — 文字を大きくし、下のフッターを消した回の記録

2026-08-12 の2回目の作業記録（1回目は9〜12月の追加＝[セッション記録_2026-08-12.md](./セッション記録_2026-08-12.md)）。
暦の計算ルールは [CLAUDE.md](./CLAUDE.md) にある。このファイルは「どう決めて、どこを直したか」の記録。

## 依頼

> 猫の暦のアプリが全体的に字が小さいので大きくしてください。下の構成はなくしてください

## 本体を見つけるまで

Claude Code の作業ディレクトリは `C:\Users\User\OneDrive\デスクトップ\AI\codex` で、ここには **`猫の暦QRコード.png` と `猫の暦アイコン.png` しかない**。前回と同じつまずき方をした。

探索の経緯：デスクトップ配下 → `AIの作業場`（家計簿・タロット・食事記録はあるが暦はない）→ ホーム配下の `*.html` を深さ6で列挙して `C:\Users\User\neko-koyomi\index.html` に到達。

```bash
find "c:/Users/User" -maxdepth 6 -iname "*.html" -not -path "*/node_modules/*" -not -path "*/AppData/*"
```

**次からは最初に `C:\Users\User\neko-koyomi` を見る。** codex フォルダは成果物の置き場で、ソースではない。

## 「字を大きく」をどう解いたか

このHTMLは font-size をすべて px 直書きしている（`body` の 15px 以下、60か所弱）。したがって `body` の1か所だけ上げても効かない。

検討した3案：

| 案 | 判断 |
| --- | --- |
| 全部を rem に変換し `html{font-size}` で一括調整 | 将来は楽だが、60か所の換算でミスが出やすい。今回の目的（読みやすくする）には過剰 |
| `body` に `zoom` / `transform: scale` | レイアウトの計算が崩れる。カレンダーのグリッドに響く |
| **各 px を直接引き上げる**（採用） | 差分が読める。小さい字ほど強めに上げる、という調整ができる |

**小さい字ほど強めに上げる**方針にした。読みにくさは注記類（10〜12px）に集中していて、見出しはもともと十分大きいため。倍率でなく、以下の実寸で割り当てた。

| 元 | 後 | 例 |
| --- | --- | --- |
| 9.5〜10px | 11〜11.5px | タグ、旧暦、セル下段の注記、アプリバー副題 |
| 10.5〜12px | 12〜14px | 見出しラベル、凡例、詳細欄の本文、暦のことばの語釈 |
| 12.5〜13.5px | 14〜15.5px | ボタン、行動リスト、曜日、詳細欄の副題 |
| 15〜17px | 17〜19px | 本文、月タブ、アプリ名、詳細欄の値 |
| 19〜26px | 21〜28px | カレンダーの日付、詳細の日付 |
| 32px・clamp | 34px・clamp(21,2.6vw,27) | 本日の暦の日付、テーマ文 |

見出し `h1`（clamp 40〜68px）と、`em` 指定の曜日（`.58em` / `.6em`）は元のまま。

実行は行番号指定の sed スクリプト（57行）で一括。行数が変わらない置換なので行番号がずれない。

```
134s/font-size: 15px;/font-size: 17px;/
162s/font-size: 13px;/font-size: 14px;/
…
```

## 文字を大きくしたあとに必要だった調整

字だけ上げると入れ物が足りなくなるので、4か所を追従させた。

| 何 | 変更 | 理由 |
| --- | --- | --- |
| `.cell` の最小高さ | 118px → 132px | セル内が日付＋六曜＋行事＋タグで縦に伸びるため |
| `.cell`（700px以下） | 92px → 104px | 同上 |
| `.cols` の右列 | 306px → 336px | 詳細欄の本文が14pxになり、折り返しが増えるため |
| `.feature` の左右列 | 218px / 214px → 238px / 234px | 本日の暦の日付・開運欄が詰まるため |

### スマホ幅でセル見出しが割れる問題

iPhone 幅 390px での実寸：

```
390 − wrap左右padding 28 = 362
362 − border 2 − gap 6 = 354 → 7列なので1列 50.5px
50.5 − cell左右padding 10 = 内側 約40px
```

ここに日付（25px の明朝2桁 ≒ 23px）と六曜（13.5px×2文字＋padding ≒ 33px）を `gap: 5px` で横並びにしていた。**元の 19px / 11px でも既に収まっておらず**、六曜が「大／安」と縦に割れる状態だった。文字を上げるとこれが悪化する。

対処：700px以下で見出しを折り返し可にし、六曜そのものは割らせない。

```css
@media (max-width: 700px) {
  .cell-head { flex-wrap: wrap; gap: 1px 4px; }
  .roku { font-size: 12.5px; padding: 0 3px; white-space: nowrap; }
}
```

入りきらない場合、六曜は縦に割れず次の行へ落ちる。セル高さを104pxにしてあるので1行増えても収まる。

## 「下の構成」の解釈

`footer.src` の1文目が「**構成は**「ChatGPTからのメモ2.txt」に挙げられた要素（…）に沿っています」で始まっており、これが依頼の「下の構成」にあたる。まずこの一文だけを消し、残り（解釈である旨の断り＋出典リンク）をどうするか確認したところ、**フッターごと全部消す**との回答だったので `<footer class="src">` ブロックと、使われなくなった `footer.src` のCSS 2行も削除した。

出典（国立天文台 令和8年暦要項／Magic Wands／一粒万倍日一覧）は CLAUDE.md に残っているので、消えたわけではない。CLAUDE.md の「ページ下部にもその旨を明記してある」という記述は実態と合わなくなったため書き換えた。

## 変更したファイル

```
index.html   font-size 57か所、レイアウト4か所、@700に2行追加、footerブロック削除、footer.srcのCSS削除
sw.js        CACHE を neko-koyomi-v2 → v3
CLAUDE.md    文字サイズの節を追加、フッターに関する記述を訂正
```

差分は 64 insertions / 71 deletions。

## この回のコミット

```
6662fca 文字を全体的に大きくし、ページ下部のフッターを削除
```

push 済み。GitHub Pages への反映は1〜2分。ホーム画面に追加済みの端末は、`sw.js` を v3 に上げてあるので一度閉じて開き直せば新しい画面になる。

## まだ小さいと言われたら

- 全体をもう一段上げる：同じやり方で各 px に +1〜2px。ただし**カレンダーのセルが先に限界**なので、`.cell` の最小高さと、スマホ幅での折り返しを必ず一緒に見る。
- 本文だけ上げる：`body` の 17px と `.d-row p` / `.d-list li`（14px）だけを上げれば、カレンダーの見た目を変えずに読み物部分が大きくなる。
- 端末側の文字サイズ設定に追従させたい場合は、px を rem に置き換える改修が要る（今回は見送った案）。
